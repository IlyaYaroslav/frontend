services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: frontend:latest
    container_name: frontend
    restart: unless-stopped
    ports:
      - "8080:80"
    networks:
      - taskspace-net

networks:
  taskspace-net:
    external: true
